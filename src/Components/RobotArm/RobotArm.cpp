#include "RobotArm.h"

RobotArm::RobotArm()
{
    //m_servoC1Base.attach(m_servo1Base);
    //m_servoC2.attach(m_servo2);
    //m_servoC3.attach(m_servo3);
    //m_servoC4.attach(m_servo4);
}

RobotArm::~RobotArm()
{
}

void RobotArm::Initialize(){
    /*m_servoC1Base.attach(m_servo1Base);
    m_servoC2.attach(m_servo2);
    m_servoC3.attach(m_servo3);
    m_servoC4.attach(m_servo4);*/
}